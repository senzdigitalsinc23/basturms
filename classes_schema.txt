Array
(
    [Table] => classes
    [Create Table] => CREATE TABLE `classes` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `class_id` varchar(10) NOT NULL,
  `class_name` varchar(100) NOT NULL,
  `level_id` varchar(10) DEFAULT NULL,
  `status` enum('active','inactive') NOT NULL,
  `added_by` varchar(20) NOT NULL,
  `added_on` datetime NOT NULL DEFAULT current_timestamp(),
  PRIMARY KEY (`id`),
  UNIQUE KEY `idx_classes_class_id` (`class_id`),
  KEY `idx_classes_class_name` (`class_name`),
  KEY `fk_classes_level_id` (`level_id`),
  CONSTRAINT `fk_classes_level_id` FOREIGN KEY (`level_id`) REFERENCES `class_levels` (`level_code`) ON DELETE SET NULL ON UPDATE CASCADE
) ENGINE=InnoDB AUTO_INCREMENT=47 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci
)
